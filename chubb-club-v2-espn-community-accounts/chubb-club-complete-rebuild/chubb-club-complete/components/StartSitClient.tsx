'use client';import {useState} from 'react';import PlayerSearch from './PlayerSearch';import type {Player} from '@/lib/types';
export default function StartSitClient(){const [a,setA]=useState<Player|null>(null),[b,setB]=useState<Player|null>(null);return <><div className="two"><div className="card"><h3>Player A</h3><PlayerSearch label="First player" onPick={setA}/></div><div className="card"><h3>Player B</h3><PlayerSearch label="Second player" onPick={setB}/></div></div>{a&&b&&<div className="card compareCard"><h3>{a.name} vs. {b.name}</h3><p className="muted">Both players are now linked to their live Chubb Club player records. Rankings, matchup and stat scoring will populate from the synced data instead of typed text.</p><div className="two"><div><b>{a.name}</b><p>{a.team} • {a.position}</p></div><div><b>{b.name}</b><p>{b.team} • {b.position}</p></div></div></div>}</>}
