'use client';
import {useState} from 'react';import {browserSupabase} from '@/lib/supabase';import {useRouter} from 'next/navigation';
export default function AuthForm({mode}:{mode:'login'|'signup'}){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[username,setUsername]=useState(''),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();async function go(){const s=browserSupabase();if(!s){setMsg('Supabase is not connected on Render.');return}if(!email||password.length<6){setMsg('Enter your email and a password with at least 6 characters.');return}if(mode==='signup'&&!/^[a-zA-Z0-9_]{3,24}$/.test(username)){setMsg('Username must be 3–24 characters using letters, numbers or _.');return}setBusy(true);const r=mode==='signup'?await s.auth.signUp({email,password,options:{data:{username,display_name:username}}}):await s.auth.signInWithPassword({email,password});setBusy(false);if(r.error){setMsg(r.error.message);return}if(mode==='signup'){if(r.data.session){router.push('/account');router.refresh()}else setMsg('Account created. Check your email to confirm it, then log in.');}else{router.push('/account');router.refresh()}}return <div className="card authCard"><h2>{mode==='login'?'Log in':'Create account'}</h2>{mode==='signup'&&<input className="input" autoComplete="username" placeholder="Username" value={username} onChange={e=>setUsername(e.target.value)}/>}<input className="input" type="email" autoComplete="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" type="password" autoComplete={mode==='login'?'current-password':'new-password'} placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn" disabled={busy} onClick={go}>{busy?'Working…':mode==='login'?'Log in':'Sign up'}</button>{msg&&<p className="muted">{msg}</p>}</div>}
