'use client';import {useState} from 'react';import {browserSupabase} from '@/lib/supabase';
export default function AuthForm({mode}:{mode:'login'|'signup'}){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function go(){const s=browserSupabase();if(!s){setMsg('Supabase is not connected yet. Add the two NEXT_PUBLIC_SUPABASE environment variables on Render.');return}const r=mode==='signup'?await s.auth.signUp({email,password}):await s.auth.signInWithPassword({email,password});setMsg(r.error?r.error.message:(mode==='signup'?'Account created. Check your email if confirmation is enabled.':'Signed in.'))}return <div className="card" style={{maxWidth:480,margin:'30px auto'}}><h2>{mode==='login'?'Log in':'Create account'}</h2><input className="input" style={{width:'100%',marginBottom:10}} placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" style={{width:'100%',marginBottom:10}} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn" onClick={go}>{mode==='login'?'Log in':'Sign up'}</button>{msg&&<p className="muted">{msg}</p>}</div>}
