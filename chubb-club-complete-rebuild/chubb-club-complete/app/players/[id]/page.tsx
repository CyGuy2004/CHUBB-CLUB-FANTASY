import {getPlayer,currentWeek} from '@/lib/players';import {notFound} from 'next/navigation';
export default async function Page({params}:{params:{id:string}}){const p=await getPlayer(params.id);if(!p)notFound();return <section className="wrap section"><div className="profileHero">{p.headshot?<img className="profileShot" src={p.headshot} alt=""/>:<div className="profileShot"/>}<div><div className="eyebrow">{p.team} • {p.position}</div><h1>{p.name}</h1><p className="muted">Week {currentWeek()} • {p.injuryStatus||'No injury designation in player feed'}</p></div></div><div className="two"><div className="card"><h3>Current Season Stats</h3><p className="muted">This panel is provider-ready. Connect the approved stats feed in environment settings to populate 2026 stats automatically.</p></div><div className="card"><h3>Week {currentWeek()} Matchup</h3><p className="muted">Weekly opponent, game time and matchup data will populate from the connected schedule/stat provider.</p></div><div className="card"><h3>Latest News</h3><p className="muted">Player news is intentionally not fabricated. A licensed/supported news feed can populate this panel.</p></div><div className="card"><h3>Rankings</h3><p className="muted">ESPN/reference • Chubb Club Consensus • Cyrus • Aiden • Gerardo • Nehemiah</p></div></div></section>}
