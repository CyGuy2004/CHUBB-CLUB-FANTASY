import {NextResponse} from 'next/server';import {playerMetrics,trending,type Scoring} from '@/lib/fantasy';
export async function GET(req:Request){const u=new URL(req.url);const raw=u.searchParams.get('scoring')||'ppr';const scoring=(['ppr','half','standard'].includes(raw)?raw:'ppr') as Scoring;const [metrics,adds,drops]=await Promise.all([playerMetrics(scoring),trending('add',30),trending('drop',20)]);const projected=metrics.filter(x=>x.projection>0).length;return NextResponse.json({metrics,adds,drops,status:{projectedPlayers:projected,source:metrics.find(x=>x.source==='ESPN')?'ESPN Fantasy + fallback':'fallback fantasy feed'}},{headers:{'Cache-Control':'public, s-maxage=900, stale-while-revalidate=1800'}})}
