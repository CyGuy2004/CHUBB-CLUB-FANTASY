import {NextResponse} from 'next/server';
export async function GET(){try{const r=await fetch('https://site.api.espn.com/apis/site/v2/sports/football/nfl/teams',{next:{revalidate:86400}});if(!r.ok)throw new Error();const d=await r.json();const teams=(d.sports?.[0]?.leagues?.[0]?.teams||[]).map((x:any)=>x.team||x).map((t:any)=>({abbr:t.abbreviation||'',name:t.displayName||t.name||'',logo:t.logos?.[0]?.href||t.logo||''})).filter((t:any)=>t.abbr&&t.name).sort((a:any,b:any)=>a.name.localeCompare(b.name));return NextResponse.json({teams})}catch{return NextResponse.json({teams:[]})}}
