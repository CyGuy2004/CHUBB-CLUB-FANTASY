'use client';import {useEffect,useState} from 'react';import PlayerSearch from './PlayerSearch';import PlayerAvatar from './PlayerAvatar';import type {Player} from '@/lib/types';
type M={player:Player;projection:number;seasonPoints:number;value:number;rank:number;positionRank:number;line?:{stats:Record<string,number>}};
export default function StartSitClient(){const [a,setA]=useState<Player|null>(null),[b,setB]=useState<Player|null>(null),[score,setScore]=useState('ppr'),[rows,setRows]=useState<M[]>([]),[loading,setLoading]=useState(false);useEffect(()=>{setLoading(true);fetch(`/api/fantasy?scoring=${score}`).then(r=>r.json()).then(d=>setRows(d.metrics||[])).finally(()=>setLoading(false))},[score]);const metric=(p:Player|null)=>rows.find(x=>x.player.id===p?.id);const A=metric(a),B=metric(b);const winner=A&&B?(A.projection===B.projection?null:A.projection>B.projection?A:B):null;return <><div className="toolBar"><label>Scoring <select className="select" value={score} onChange={e=>setScore(e.target.value)}><option value="ppr">PPR</option><option value="half">Half PPR</option><option value="standard">Standard</option></select></label></div><div className="two"><div className="card"><h3>Player 1</h3><PlayerSearch label="Search player" onPick={setA}/></div><div className="card"><h3>Player 2</h3><PlayerSearch label="Search player" onPick={setB}/></div></div>{a&&b&&<div className="analysisPanel"><div className="verdict"><span>CHUBB CLUB START/SIT</span><h2>{loading?'Updating projections…':winner?`START ${winner.player.name.toUpperCase()}`:'TOO CLOSE TO CALL'}</h2><p>Based on current-week projection, season production and positional rank. Re-check near kickoff for injuries and role changes.</p></div><div className="compareGrid">{[A,B].map((m,i)=>m?<div className={`comparePlayer ${winner?.player.id===m.player.id?'recommended':''}`} key={m.player.id}><PlayerAvatar p={m.player} className="compareShot"/><h3>{m.player.name}</h3><div className="muted">{m.player.team} • {m.player.position}{m.player.injuryStatus?` • ${m.player.injuryStatus}`:''}</div><div className="metricBig">{m.projection.toFixed(1)} <small>proj pts</small></div><div className="statRows"><span>Overall rank <b>#{m.rank}</b></span><span>{m.player.position} rank <b>#{m.positionRank}</b></span><span>Season fantasy pts <b>{m.seasonPoints.toFixed(1)}</b></span></div></div>:<div className="comparePlayer" key={i}>Projection unavailable</div>)}</div></div>}</>}
