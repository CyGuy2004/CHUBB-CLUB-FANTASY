'use client';import {useState} from 'react';import {browserSupabase} from '@/lib/supabase';import {useRouter} from 'next/navigation';
export default function AuthForm({mode}:{mode:'login'|'signup'}){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[username,setUsername]=useState(''),[msg,setMsg]=useState('');const router=useRouter();async function go(){const s=browserSupabase();if(!s){setMsg('Supabase is not connected.');return}if(mode==='signup'&&!/^[a-zA-Z0-9_]{3,24}$/.test(username)){setMsg('Username must be 3–24 characters using letters, numbers or _.');return}const r=mode==='signup'?await s.auth.signUp({email,password,options:{data:{username,display_name:username}}}):await s.auth.signInWithPassword({email,password});if(r.error){setMsg(r.error.message);return}setMsg(mode==='signup'?'Account created. Check your email if confirmation is enabled.':'Signed in.');if(mode==='login'){router.push('/account');router.refresh()}}return <div className="card authCard"><h2>{mode==='login'?'Log in':'Create account'}</h2>{mode==='signup'&&<input className="input" placeholder="Username" value={username} onChange={e=>setUsername(e.target.value)}/>}<input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input className="input" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/><button className="btn" onClick={go}>{mode==='login'?'Log in':'Sign up'}</button>{msg&&<p className="muted">{msg}</p>}</div>}
